// The one place to change the public origin and path.
// Canonical URLs, the sitemap and every internal link derive from these values.
export const SITE_ORIGIN = 'https://yuvicc.dev';
export const BASE_PATH = '/llvm/';
